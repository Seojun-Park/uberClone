import React, { FC, ReactElement } from 'react';

type InputProps = {
    defaultValue: [any]
}

const useInput = (defaultValue) => {
    return (
        <>
            input
        </>
    )
}

export default useInput